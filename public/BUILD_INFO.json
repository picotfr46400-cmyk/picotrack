{
  "version": "v22-env-license-resolution",
  "date": "2026-06-17",
  "changes": [
    "fixed license limit environment resolution for GLOBAL super_admin accounts",
    "license screens now use runtime/domain environment instead of profile GLOBAL when applicable",
    "users screen limit reads PROSPECT/DEMO/PROD from domain-aware bootstrap",
    "sidebar environment label prepared for runtime environment display",
    "cache version bumped to avoid stale service worker assets"
  ],
  "securityNote": "GLOBAL remains a platform role/license scope only; active environment is resolved from host/runtime configuration."
}
